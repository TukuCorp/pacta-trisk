% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact_catalog.R
\name{artifact_path}
\alias{artifact_path}
\title{Resolve one Artifact location by key.}
\usage{
artifact_path(cfg, key, sector = NULL, where = "output")
}
\arguments{
\item{cfg}{list — the loaded engagement config.}

\item{key}{character(1) — a catalog key.}

\item{sector}{character(1)|NULL — the sector for a sector-scoped key; NULL
returns every configured sector, in \code{cfg$trisk_sectors} order.}

\item{where}{character(1) — "output" (the producer's own location) or
"snapshot" (the location the dashboard reads).}
}
\value{
character — the repo-relative path(s). Length 1 for an
engagement-scoped key; length \code{length(cfg$trisk_sectors)} for a
sector-scoped key resolved without a sector.
}
\description{
Resolve one Artifact location by key.
}
