% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact_catalog.R
\name{artifact_catalog}
\alias{artifact_catalog}
\title{The resolved Artifact catalog for one engagement.}
\usage{
artifact_catalog(cfg)
}
\arguments{
\item{cfg}{list — the loaded engagement config.}
}
\value{
data.frame with the columns of \code{.artifact_catalog_columns()}, in
that order: engagement rows first, then sector rows grouped by sector.
}
\description{
Builds the engagement-scoped rows, then one sector-scoped block per sector
in \code{cfg$trisk_sectors} order, so the row order is deterministic (the JSON
export and the byte-identity gate both depend on that).
}
