% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact_catalog.R
\name{write_artifact_catalog_json}
\alias{write_artifact_catalog_json}
\title{Write the machine-readable catalog export the dashboard reads.}
\usage{
write_artifact_catalog_json(cfg, path)
}
\arguments{
\item{cfg}{list — the loaded engagement config.}

\item{path}{character(1) — where to write the JSON.}
}
\value{
invisible(path).
}
\description{
Only rows with a Snapshot location are exported, and \code{snapshot_path} is
relative to the Snapshot root (the \verb{dashboard/data/} prefix is stripped).
The file carries no timestamp and its rows follow catalog order, so two
runs produce byte-identical bytes -- the gate compares it like any other
Snapshot artifact.
}
